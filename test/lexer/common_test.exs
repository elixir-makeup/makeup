defmodule MakeupTest.Lexer.CommonTest do
  # use ExUnit.Case
  # use ExUnitProperties
  # use ExSpirit.Parser, text: true
  # alias Makeup.Lexer.Common, as: C
  # require C

  # test "words - recognizes word" do
  #   assert %{error: nil, result: "abc", rest: "x"} = parse("abcx", C.words(["abc"]))
  #   assert %{error: nil, result: "abc", rest: "x"} = parse("abcx", C.words_using_symbols(["abc"]))

  #   assert %{error: nil, result: "o'7", rest: "/"} = parse("o'7/", C.words(["o'7"]))
  #   assert %{error: nil, result: "o'7", rest: "/"} = parse("o'7/", C.words_using_symbols(["o'7"]))

  #   assert %{error: nil, result: "X% 0", rest: "-"} = parse("X% 0-", C.words(["X% 0"]))
  #   assert %{error: nil, result: "X% 0", rest: "-"} = parse("X% 0-", C.words_using_symbols(["X% 0"]))
  # end
end